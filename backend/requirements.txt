# FastAPI and server
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
aiofiles==23.2.1

# Audio processing - simplified for free tier
pydub==0.25.1
librosa==0.10.1
soundfile==0.12.1
scipy==1.12.0
numpy>=1.24.0

# Utilities
pydantic==2.6.1
pydantic-settings==2.1.0
python-dotenv==1.0.1

# Note: demucs, torch, torchaudio, pyannote.audio are commented out
# because they require too much memory for Render free tier
# The app will use fallback separation methods instead

# Uncomment these for full functionality on paid tier:
# torch>=2.0.0
# torchaudio>=2.0.0
# demucs==4.0.1
# pyannote.audio==3.1.1
